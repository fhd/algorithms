script(src="js/sorting/display.js")

script
    var currentAlgorithmFile = "!{currentAlgorithm.file}";
    var currentAlgorithmFunction = "!{currentAlgorithm.functionName}";
    $(init);

h2= currentAlgorithm.name

p= currentAlgorithm.description

canvas(id="canvas", width="305", height="155")
    | Sorry, your browser doesn't support HTML5 Canvas.
div
    input(type="button", id="shuffle", value="Shuffle")
    input(type="button", id="sort", value="Sort")

pre#code.prettyprint.lang-js= currentAlgorithm.code
